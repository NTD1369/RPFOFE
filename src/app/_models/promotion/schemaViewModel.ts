import { SSchemaLine } from "./promotionchemaline";
import { SPromoSchema } from "./promotionschema";

export class SchemaViewModel extends SPromoSchema {
    SchemaViewModel;
    SchemaLines: SSchemaLine[];
}