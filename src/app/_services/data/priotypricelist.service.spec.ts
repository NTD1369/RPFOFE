/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PriotypricelistService } from './priotypricelist.service';

describe('Service: Priotypricelist', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PriotypricelistService]
    });
  });

  it('should ...', inject([PriotypricelistService], (service: PriotypricelistService) => {
    expect(service).toBeTruthy();
  }));
});
