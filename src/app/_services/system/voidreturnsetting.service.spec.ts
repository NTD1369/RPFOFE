/* tslint:disable:no-unused-variable */

import { TestBed, inject, waitForAsync } from '@angular/core/testing';
import { VoidreturnsettingService } from './voidreturnsetting.service';

describe('Service: Voidreturnsetting', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [VoidreturnsettingService]
    });
  });

  it('should ...', inject([VoidreturnsettingService], (service: VoidreturnsettingService) => {
    expect(service).toBeTruthy();
  }));
});
